// function postListContent({posts, deletePost}) {
//     if (posts.length > 0) {
//         return {
//             {postArr.map((elem, index) => <PostItem deletePost={deletePost} key={elem.id} index={index} post={elem}/>) }
//         }
//     }
// }